<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required Permissions -->
    <!-- Permission to access the internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Permission to access the camera for video calls -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Permission to record audio for voice in video calls -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Permission to check network state -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Optional: Prevent the screen from turning off during video chat -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Application block -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OldSchool"
        tools:targetApi="31">

        <!-- Main activity declaration -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.OldSchool">

            <!-- Launcher activity intent filter -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
